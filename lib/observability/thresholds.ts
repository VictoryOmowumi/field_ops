export const SLOW_QUERY_WARNING_MS = 2000;
export const SLOW_QUERY_CRITICAL_MS = 5000;

/** Default guess for Supabase free-tier storage bucket quota (~1GB) — tune once actual plan quota is confirmed. */
export const STORAGE_WARNING_GB = 0.8;
